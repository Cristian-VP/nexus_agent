# Dependencias Node (si alguien clona y hace npm install)
node_modules/

# Variables de entorno con secretos — NUNCA al repositorio
.env

# Datos persistentes del agente (generados en runtime)
config/
workspace/

# Dependencias y builds de los nuevos microservicios
backend-bridge/node_modules/
backend-bridge/dist/
frontend-ui/node_modules/
frontend-ui/.next/
frontend-ui/out/
*.tsbuildinfo

# Excepciones: mantener los marcadores de directorio
!config/.gitkeep
!workspace/.gitkeep

# Logs y temporales
*.log
.DS_Store
